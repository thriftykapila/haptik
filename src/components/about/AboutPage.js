import React from "react";

const AboutPage = () => (
  <div>
    <h2>About</h2>
    <p>
    Create a React Component that allows number selection through a draggable slider. The draggable slider  should be a controlled input. Sensible props must be added to make the slider reusable across different  scenarios.  ​Avoid using any external UI libraries and packages
    </p>
  </div>
);

export default AboutPage;
